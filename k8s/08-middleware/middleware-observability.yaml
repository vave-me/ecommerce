# --- Strip Prefix Middleware for Observability Services ---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-prefix
  namespace: observability
spec:
  stripPrefix:
    prefixes:
      - /jaeger
      - /grafana
      - /prometheus