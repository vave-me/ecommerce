apiVersion: v1
kind: Secret
metadata:
  name: categories-secrets
  namespace: my-project # Ensure this namespace exists
type: Opaque
data:
  # Password: "categories_pass"
  CATEGORIES_PASS: REDACTED
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: categories
  namespace: my-project
  labels:
    app: categories
spec:
  replicas: 1
  selector:
    matchLabels:
      app: categories
  template:
    metadata:
      labels:
        app: categories
    spec:
      initContainers:
        - name: wait-for-db
          image: postgres:16-alpine
          env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: categories-secrets
                  key: CATEGORIES_PASS
          command: [ 'sh', '-c' ]
          args:
            - |
              set -e
              echo "Waiting for postgres service (postgres.database.svc.cluster.local:5432) to accept connections for user categories_user..."
              until psql -h postgres.database.svc.cluster.local -U categories_user -d categories -c "SELECT 1;" > /dev/null 2>&1; do
                echo "Postgres not ready yet for user categories_user, sleeping for 2 seconds..."
                sleep 2
              done
              echo "Postgres service is ready for user categories_user!"
          resources:
            requests:
              cpu: "25m"
              memory: "32Mi"
            limits:
              cpu: "100m"
              memory: "64Mi"
        - name: wait-for-nats
          image: busybox:1.36
          command: [ 'sh', '-c', 'echo "Waiting for nats (nats.messaging.svc.cluster.local:4222)..."; until nc -zv nats.messaging.svc.cluster.local 4222; do sleep 2; done; echo "NATS ready!"' ]
          resources:
            requests:
              cpu: "10m"
              memory: "16Mi"
            limits:
              cpu: "50m"
              memory: "32Mi"
      containers:
        - name: categories
          image: registry.sfx-markt.de/categories:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
            - containerPort: 9000
              name: grpc
          envFrom:
            - configMapRef:
                name: common-config
            - secretRef:
                name: common-secrets
          env:
            - name: PG_CONN
              value: "host=$(POSTGRES_HOST) port=$(POSTGRES_PORT) dbname=categories user=categories_user search_path=categories,public sslmode=disable password=categories_pass"
            - name: PGHOST
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: POSTGRES_HOST
            - name: PGPORT
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: POSTGRES_PORT
            - name: PGDATABASE
              value: "categories"
            - name: PGUSER
              value: "categories_user"
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: categories-secrets
                  key: CATEGORIES_PASS
            - name: PGSSLMODE
              value: "disable"
            - name: RPC_SERVICES
              value: "CATEGORIES=categories.my-project.svc.cluster.local:9000"
            - name: OTEL_SERVICE_NAME
              value: "categories"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.observability.svc.cluster.local:4317"
            - name: OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: OTEL_TRACES_EXPORTER
              value: "otlp"
            - name: OTEL_METRICS_EXPORTER
              value: "otlp"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: categories
  namespace: my-project
  labels:
    app: categories
spec:
  selector:
    app: categories
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: grpc-internal
      port: 9000
      targetPort: grpc
      # nodePort: 30004 # This is still part of your service, but Traefik will route to service port 9000.
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: categories-external # External service name
  namespace: my-project
  labels:
    app: categories
spec:
  selector:
    app: categories
  ports:
    - name: grpc-external
      protocol: TCP
      port: 30004 # External port
      targetPort: grpc
  type: LoadBalancer
