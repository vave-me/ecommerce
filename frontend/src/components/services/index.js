// Export both client and server versions of ServiceCard
export { default as ServiceCard } from './ServiceCard';
export { default as ServiceCardServer } from './ServiceCard.server';

// Default export is the client component for backward compatibility
export { default } from './ServiceCard'; 