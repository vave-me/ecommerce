# redis.conf

# Enable append-only mode for data persistence
appendonly yes

# Set a strong password for Redis
requirepass YourStrongPasswordHere

# Optionally, you can keep protected mode enabled
protected-mode yes

# Bind to all interfaces or specific ones
bind 0.0.0.0
